import joblib
import pandas as pd

# 1. Guardar el Modelo Final (Asumimos el Random Forest Optimizado)
joblib.dump(best_rf_model, "modelo_churn_final.pkl")
print("✅ Modelo final guardado como modelo_churn_final.pkl")

# 2. Guardar el Scaler (Objeto MinMaxScaler)
joblib.dump(scaler, "scaler_minmax.pkl")
print("✅ Scaler guardado como scaler_minmax.pkl")

# 3. Guardar las Columnas de Entrenamiento
# Esto asegura que el nuevo cliente tenga las mismas columnas OHE en el orden correcto
joblib.dump(X.columns.tolist(), "columnas_entrenamiento.pkl")
print("✅ Columnas guardadas como columnas_entrenamiento.pkl")

# Título de Streamlit y widgets...
st.title("Predicción de Churn de Clientes Telco")

# ...
